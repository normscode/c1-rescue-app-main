import 'package:flutter/material.dart';

/* Light and Dark Outlined Button Themes */
class RIconButtonTheme {
  RIconButtonTheme._(); // To Avoid Creating Instances

  /* -- Light Theme -- */
  static const lightIconButtonTheme = IconThemeData(color: Color(0xFF000000));

  /* -- Dark Theme -- */
  static const darkIconButtonTheme = IconThemeData(color: Color(0xffffffff));
}
